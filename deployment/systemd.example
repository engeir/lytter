# Example systemd service for Last.fm stats app
# Save as /etc/systemd/system/lytter.service

[Unit]
Description=Last.fm Stats Web Application
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/path/to/lytter
Environment=PATH=/path/to/lytter/.venv/bin
ExecStart=/usr/bin/uv run uvicorn simple_app:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target

# To install and start:
# sudo systemctl enable lytter.service
# sudo systemctl start lytter.service
