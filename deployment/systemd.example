# Example systemd service for Last.fm stats app
# Save as /etc/systemd/system/lastfm-stats.service

[Unit]
Description=Last.fm Stats Web Application
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/path/to/lastfm-stats
Environment=PATH=/path/to/lastfm-stats/.venv/bin
ExecStart=/usr/bin/uv run uvicorn simple_app:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target

# To install and start:
# sudo systemctl enable lastfm-stats.service
# sudo systemctl start lastfm-stats.service
